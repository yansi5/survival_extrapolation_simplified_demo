% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_survival.R
\name{plot_survival}
\alias{plot_survival}
\title{Plot observed and extrapolated survival curves}
\usage{
plot_survival(fits, data, times)
}
\arguments{
\item{fits}{A fitted-model object returned by
\code{\link[=fit_survival_models]{fit_survival_models()}}.}

\item{data}{A data frame containing \code{time}, \code{status}, and \code{treat}.}

\item{times}{A numeric vector of non-negative time points used for
parametric survival prediction and extrapolation.}
}
\value{
A \code{ggplot2} plot object.
}
\description{
Plots observed Kaplan-Meier survival estimates together with fitted
parametric survival curves. Treatment arms are shown in separate panels.
}
\examples{
lung_data <- data.frame(
  time = survival::lung$time,
  status = survival::lung$status - 1,
  treat = survival::lung$sex
)

fits <- fit_survival_models(
  lung_data,
  dists_to_fit = c("weibull", "lnorm")
)

plot_survival(
  fits,
  data = lung_data,
  times = seq(0, 1500, by = 50)
)
}
